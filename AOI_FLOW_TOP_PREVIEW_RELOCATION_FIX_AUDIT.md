# AOI FLOW Top Preview Relocation Fix Audit

## 目的
下部操作エリア内に散らばっていた確認用プレビューを、画面上部の `EDIT PREVIEW` に集約する。
下部は操作項目、上部は確認画面として扱う。

## 変更ファイル
- `app/flow/drafts/new/page.tsx`

## 実装内容
- 上部プレビューを現在の操作タブに連動
  - 素材タブ: 元画像 / アップロード画像を表示
  - 背景タブ: テンプレ背景 / AI背景を表示
  - 合成タブ: 保存済み合成画像、または背景＋商品画像の簡易ライブプレビューを表示
  - 動画タブ: 動画URLがある場合は video preview を表示
- 既存の文章作成枠は維持
- 既存の素材 / 背景 / 合成 / 動画タブは維持
- Firestore構造変更なし
- API変更なし
- 既存コンポーネント削除なし

## 監査
- 削除ファイルなし
- DB構造変更なし
- API変更なし
- UIレイアウトのみ変更

## 注意
この環境では `next` が未インストールのため `npm run build` は実行できませんでした。
エラー: `next: not found`
